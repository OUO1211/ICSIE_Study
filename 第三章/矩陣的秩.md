---
subject: Linear Algebra
chapter: 3
section: 3-4
status: finished
tags: [秩, rank, nullity, Sylvester]
core_mapping: "rank=dim(RS)=dim(CS)，Sylvester n=rank+nullity"
mantra: "列秩行秩必相等，加上 nullity 等行數"
pattern_signal_strong:
  - condition: "題目要用 Sylvester 定理推算 nullity 或 dim"
    weight: 3
  - condition: "出現 rank(AB)≤min(rank(A),rank(B)) 的不等式"
    weight: 3
pattern_signal_weak:
  - condition: "出現 AB=O 時 rank(A)+rank(B)≤n 的推論"
    weight: 1
  - condition: "出現 rank-1 矩陣 A=xy^T 的性質"
    weight: 1
anti_signal:
  - condition: "rank(A+B) ≤ rank(A)+rank(B)（加法上界，不是等式）"
  - condition: "P,Q 可逆時 rank(PA)=rank(AQ)=rank(A)（可逆矩陣不改秩）"
---
	
> **第三章 § 3-4｜理論定義**：以子空間維度定義 rank，證明列秩 = 行秩，導出 Sylvester 定理。
> 第一章的操作定義（列運算版）見 [[秩Rank]]（§ 1.6）。

# 矩陣的秩（Rank）

## 定義

對矩陣 $A$：

- **列秩**（row rank）：$rr(A) = \dim(RS(A))$
- **行秩**（column rank）：$cr(A) = \dim(CS(A))$

## 核心定理：列秩 = 行秩

$$\boxed{rr(A) = cr(A) = \text{rank}(A)}$$

## Sylvester 定理（Rank-Nullity Theorem）

設 $A$ 為 $m \times n$ 矩陣：

$$\boxed{n = \text{rank}(A) + \text{nullity}(A) = \dim(CS(A)) + \dim(\ker(A))}$$
$$m = \text{rank}(A) + \dim(L\ker(A))$$

> **nullity**$(A) = \dim(\ker(A))$ = 自由變數個數

## 重要性質

設 $A$ 為 $m \times n$ 矩陣：

| 性質 | 公式 | 備注 |
|------|------|------|
| 轉置不改秩 | $\text{rank}(A) = \text{rank}(A^T)$ | 重要 |
| 上界 | $\text{rank}(A) \leq \min\{m, n\}$ | |
| 運算不改秩 | $A \sim B \Rightarrow \text{rank}(A) = \text{rank}(B)$ | 列/行運算 |
| 乘積上界 | $\text{rank}(AB) \leq \min\{\text{rank}(A), \text{rank}(B)\}$ | 很重要 |
| 可逆矩陣不改秩 | $P, Q$ 可逆時，$\text{rank}(PA) = \text{rank}(AQ) = \text{rank}(A)$ | 重要 |
| 加法上界 | $\text{rank}(A+B) \leq \text{rank}(A) + \text{rank}(B)$ | |
| 三角不等式 | $|\text{rank}(A) - \text{rank}(B)| \leq \text{rank}(A+B)$ | |

## 進階：Sylvester 不等式

$$\text{rank}(AB) \geq \text{rank}(A) + \text{rank}(B) - n$$

其中 $A: m \times n$，$B: n \times p$。

## 特殊情況

### $AB = O$ 時

$$CS(B) \subseteq \ker(A) \Rightarrow \text{rank}(A) + \text{rank}(B) \leq n$$

### $A = xy^T$（rank-1 矩陣）

設 $x \in R^m$，$y \in R^n$，均非零，則：
$$\text{rank}(xy^T) = 1, \quad CS(xy^T) = \text{span}\{x\}$$
$$\ker(xy^T) = \{v \mid y^T v = 0\}, \quad \dim(\ker(xy^T)) = n-1$$

## 秩的計算

1. 列運算到梯形，非零列數 $=$ rank
2. 行運算到梯形，非零行數 $=$ rank
3. 利用 Sylvester 定理反推

**相關**：[[四大基本子空間]]、[[秩與線性系統]]、[[四大子空間的基底]]

---

> **第一章基礎**：操作定義（列運算 → 非零列數）見 [[秩Rank]]（第一章）
> **第二章應用**：rank 與解的個數見 [[增廣矩陣與相容性]]、[[基本變數與自由變數]]
