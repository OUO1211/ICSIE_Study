第九章 高等樹結構 381
Advanced Tree Structure

比較新鍵5和位於j的父親之鍵10，因為鍵10的節點位於最小階層，且5＜10，則移動鍵 10 的元素到節點 j。接著，移動鍵7的元素到先前10的位置。最後，插入鍵 5 的元素。插入後的最小－最大堆積如下圖：
\begin{itemize}
\item[] 例：假設插入鍵值 80 的元素
因為 $80<10$ 而且 10 位於最小階層上，比較 Max－Level Node，而此節點鍵為 40 ，把鍵40的元素，移到j而且新的元素將插入到先前被這個元素所佔的節點。結果如下圖 ：
\end{itemize}