388
資料結構（含精選試題）
■■

Data Structure

比較鍵值4與最小一堆積中相對節點 i 其鍵值為19，因為19＞4，所以把19移到 j 節點中。然後使用最小一堆積的插入演算法把4插入 i 的位置，即可得到下圖的雙堆積。

例二：若插入 30 。比較 30 和相對的 i 節點 $19, \because 30>19$ ，所以 30 插入 Max－Heap j的位置，其結果如下圖所示。