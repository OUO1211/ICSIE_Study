# 線性代數反射參照表（一～三章統整）

> 協議：看到特徵 → 原子反射 → 抽象意義拼湊。禁止輸出證明或計算步驟。

| 看到 (Signal / 視覺關鍵字) | 想到 (Mapping / 原子反射) | 抽象意義 (用於拼湊) | 來源筆記 |
| :--- | :--- | :--- | :--- |
| **── 矩陣運算 ──** | | | |
| $AB \neq BA$，問乘法順序 | 矩陣乘法不可交換；$(AB)^{-1}=B^{-1}A^{-1}$；$(AB)^T=B^TA^T$；$(AB)^H=B^HA^H$ | 反序法則是非交換代數的核心限制，所有含乘積的運算都要先確認順序 | [[矩陣乘法]]、[[可逆矩陣]]、[[轉置矩陣]] |
| $AB = O$，$A$ 或 $B$ 非零？ | 零因子存在，$AB=O$ 不代表 $A=O$ 或 $B=O$ | 矩陣乘法不是整數環，消去律在不可逆時失效 | [[矩陣乘法]] |
| $AB = AC$，$A$ 狀態未知 | 不可直接消去 $A$；$A(B-C)=O$ 可能有零因子 | 等號不代表可逆，需先確認 $A$ 是否 Full Rank | [[矩陣乘法]]、[[可逆矩陣]] |
| 出現 $A^TA$ 或 $AA^T$ | 恆為對稱矩陣；$\text{tr}(A^TA)=$ 各元素平方和；具備半正定性 | 將任意矩陣轉化為具「優良幾何性質」的對稱結構 | [[轉置矩陣]]、[[對稱矩陣與斜對稱矩陣]]、[[跡數Trace]] |
| $(AB)^T$、$(kA)^H$、$(AB)^H$ | $(AB)^T=B^TA^T$；$(AB)^H=B^HA^H$；$(kA)^H=\bar{k}A^H$（**純量要取共軛**） | 複數域下共軛轉置比轉置多一層共軛，是常見筆誤點 | [[轉置矩陣]]、[[共軛轉置矩陣]] |
| **── 特殊矩陣結構 ──** | | | |
| $A^T = A$ | 對稱矩陣；$A$ 可逆則 $A^{-1}$ 亦對稱；特徵值均為實數（進階） | 對稱性在可逆運算下保持，是「穩定結構」 | [[對稱矩陣與斜對稱矩陣]]、[[可逆矩陣]] |
| $A^T = -A$ | 斜對稱；對角項**必為 $0$** | 反對稱矩陣的對角必須犧牲為零，是嚴格的結構限制 | [[對稱矩陣與斜對稱矩陣]] |
| 任意方陣 $A$，問能否分解 | $A = \dfrac{A+A^T}{2} + \dfrac{A-A^T}{2}$（對稱 + 斜對稱）= $V = W_1 \oplus W_2$ | 任何方陣都能唯一直和分解，是直和最經典的存在性例子 | [[對稱矩陣與斜對稱矩陣]]、[[直和]] |
| $A^H = A$（複數矩陣） | Hermitian；對角項**必為實數**；特徵值均為實數 | 對稱矩陣在複數域的推廣，保留了「實特徵值」這個關鍵性質 | [[Hermitian矩陣]] |
| $A^H = -A$ | 斜 Hermitian | 複數域中反自伴結構 | [[Hermitian矩陣]] |
| 嚴格上（下）三角矩陣 | 必為冪零矩陣（$A^n=O$） | 嚴格三角 → 高次冪清零，是冪零矩陣最常見的觸發來源 | [[三角矩陣]]、[[冪零矩陣]] |
| 上（下）三角矩陣相乘 / 求反矩陣 | 結果仍為上（下）三角；反矩陣亦為上（下）三角 | 三角結構在乘法和求逆下封閉，LU 分解的理論基礎 | [[三角矩陣]] |
| **── 跡數 Trace ──** | | | |
| 出現 $\text{tr}(AB)$ 或 $\text{tr}(BA)$ | $\text{tr}(AB)=\text{tr}(BA)$；但 $\text{tr}(AB)\neq\text{tr}(A)\text{tr}(B)$ | tr 的循環置換性是「無法交換但可輪換」，反證題首選工具 | [[跡數Trace]] |
| 問是否存在 $AB - BA = I$ | $\text{tr}(AB-BA)=0 \neq n$，矛盾，不存在 | tr 的線性性 + 循環性構成反證的封閉邏輯 | [[跡數Trace]] |
| $A^k = O$（冪零），問 $\text{tr}(A)$ | $\text{tr}(A)=0$（特徵值全為零） | 冪零 → 特徵值全零 → tr 必為零，三層推導可合一反射 | [[冪零矩陣]]、[[跡數Trace]] |
| **── 可逆矩陣 ──** | | | |
| $AB = I$（方陣），問 $BA = I$？ | 方陣只需驗一側：$AB=I \Leftrightarrow BA=I$ | 方陣的左逆即右逆，這是有限維空間的特殊性質 | [[可逆矩陣]] |
| 求 $(AB)^{-1}$、$(A^T)^{-1}$、$(kA)^{-1}$、$(A^k)^{-1}$ | $(AB)^{-1}=B^{-1}A^{-1}$；$(A^T)^{-1}=(A^{-1})^T$；$(kA)^{-1}=\frac{1}{k}A^{-1}$；$(A^k)^{-1}=(A^{-1})^k$ | 所有運算與求逆可交換（順序除外），是組合考題的常用拼湊件 | [[可逆矩陣]] |
| $2\times2$ 矩陣求反矩陣 | $A^{-1}=\frac{1}{ad-bc}\begin{bmatrix}d&-b\\-c&a\end{bmatrix}$（$ad-bc\neq0$） | 行列式 = 面積縮放比；= 0 代表平面被壓扁，無法還原 | [[可逆矩陣]] |
| 分塊三角矩陣求反矩陣 | 對角塊各自取逆；非對角塊：$-A_{11}^{-1}A_{12}A_{22}^{-1}$ 或 $-A_{22}^{-1}A_{21}A_{11}^{-1}$ | 分塊求逆只需對角塊可逆，化大問題為小問題 | [[分塊矩陣的反矩陣]] |
| $\text{diag}(c_1,\ldots,c_n)$ 求反矩陣 | $\text{diag}(c_1^{-1},\ldots,c_n^{-1})$（各分量直接取倒數） | 對角矩陣的逆是最簡情形，各維度完全解耦 | [[對角矩陣與單位矩陣]] |
| $A^k=O$，求 $(I-A)^{-1}$ 或 $(I+A)^{-1}$ | $(I-A)^{-1}=I+A+\cdots+A^{k-1}$；$(I+A)^{-1}=I-A+A^2-\cdots+(-A)^{k-1}$ | 冪零矩陣讓無窮幾何級數有限截斷，是構造逆的標準技巧 | [[冪零矩陣]]、[[矩陣多項式]] |
| $A^2=A$（冪等），求 $f(A)$ 或 $A^n$ | $A^k=A$（$k\geq1$）；$(A+I)^n=I+(2^n-1)A$ | 冪等矩陣將高次冪壓回自身，多項式計算只需代兩個值 | [[矩陣多項式]] |
| 求反矩陣（一般 $n$ 階方陣） | 高斯-喬登：$[A\mid I]\xrightarrow{\text{列運算}}[I\mid A^{-1}]$ | 列運算本質是左乘可逆矩陣，累積結果即為 $A^{-1}$ | [[利用列運算求反矩陣]] |
| 問 $A$ 是否可逆（七條等價） | 可逆 $\Leftrightarrow$ $Ax=0$ 只有零解 $\Leftrightarrow$ rank$(A)=n$ $\Leftrightarrow$ RREF 為 $I$ $\Leftrightarrow$ 基本矩陣之積 $\Leftrightarrow$ 存在左/右逆 $\Leftrightarrow$ $\det(A)\neq0$ | 七條等價是「可逆」的多角度觀察，拼湊時可任選最方便的切入點 | [[可逆矩陣等價條件]] |
| **── 列行運算 / 矩陣分解 ──** | | | |
| 列運算等價何操作 | **左乘**基本列矩陣；列等價 $\Leftrightarrow$ 存在可逆 $P$ 使 $B=PA$ | 每一步列運算都是可逆的左乘，保持解空間不變 | [[基本列運算]] |
| 行運算等價何操作 | **右乘**基本行矩陣；$B=AQ$ | 行運算改變行空間，與列運算對偶 | [[基本行運算]] |
| LU 分解存在條件 | 各**領導主子行列式**均非零；否則改用 PLU | 主子行列式為零 = 消去時遇到零主元，需先列置換 | [[LU分解]]、[[PLU分解]] |
| 解 $Ax=b$，$A$ 已做 LU | 兩步：$Ly=b$（前向代入）→ $Ux=y$（後向代入） | LU 把一個 $n^3$ 問題拆成兩個 $n^2$ 問題，效率核心 | [[LU分解]] |
| 主元為 $0$，LU 失效 | 先列置換得置換矩陣 $P$，$PA=LU$；$P^{-1}=P^T$（置換矩陣為正交矩陣） | 列置換不改變解，但讓消去數值穩定 | [[PLU分解]] |
| LDU 分解 | LU 後提出 $U$ 的對角項得 $D$；$L, U$ 對角項全為 $1$ | 將「大小」與「方向」分離：$D$ 記縮放，$L/U$ 記方向 | [[LDU分解]] |
| 求 rank$(A)$ | 列運算→REF，數 pivot；或 PAQ 分解讀 $r$ | rank = 線性無關的維度數，是矩陣資訊量的核心指標 | [[秩Rank]]、[[PAQ分解]] |
| PAQ 分解 | $PAQ=\begin{bmatrix}I_r&O\\O&O\end{bmatrix}$，$r=\text{rank}(A)$ | 最一般的標準形：列行同時操作，揭露矩陣的本質 rank 結構 | [[PAQ分解]] |
| **── 行列式計算 ──** | | | |
| $\det(A)=0$ | $A$ 奇異；$Ax=0$ 有非零解；$\text{rank}(A)<n$；$A$ 不可逆 | 行列式 = 零 = 空間被壓扁，資訊發生不可逆損耗 | [[行列式定義]]、[[奇異與非奇異矩陣]] |
| $\det(AB)$、$\det(A^T)$、$\det(A^{-1})$、$\det(A^k)$、$\det(kA)$ | $\det(AB)=\det(A)\det(B)$；$\det(A^T)=\det(A)$；$\det(A^{-1})=\frac{1}{\det A}$；$\det(A^k)=(\det A)^k$；$\det(kA)=k^n\det(A)$ | 行列式是乘法同態，但不是加法同態（$\det(A+B)$ 無公式） | [[行列式進階性質]] |
| $\det(A+B)$ | **不等於** $\det(A)+\det(B)$（加法陷阱） | 行列式對矩陣加法不是線性的，是高頻送分陷阱 | [[行列式進階性質]] |
| 行列式計算，某列（行）含多個零 | Laplace 展開：選**零最多**的列（行） | 零元素讓展開項消失，直接降低計算量 | [[Laplace展開法]] |
| 上/下/對角矩陣的行列式 | 對角項之積：$d_1 d_2 \cdots d_n$ | 三角矩陣的行列式只由對角決定，其餘元素無貢獻 | [[特殊矩陣的行列式]] |
| 第一型列（行）運算（對調兩列/行） | $\det$ **正負號相反** | 每次交換翻轉方向性（orientation），行列式記錄了這個符號 | [[行列式的列行運算性質]] |
| 第三型列（行）運算（加倍） | $\det$ **不變** | 平行四邊形的面積不因剪切變形而改變 | [[行列式的列行運算性質]] |
| 某列（行）全為 $0$，或兩列（行）相同 | $\det=0$ | 空間中存在線性相依方向，體積必為零 | [[行列式的列行運算性質]] |
| 分塊三角矩陣 $\begin{bmatrix}A&*\\O&B\end{bmatrix}$ | $\det=\det(A)\det(B)$（與右上塊無關） | 分塊三角的行列式只由對角塊決定，上下三角不干擾 | [[分塊三角矩陣的行列式]] |
| $\det\begin{bmatrix}A&B\\C&D\end{bmatrix}$，$A$ 可逆 | Schur 補：$\det(A)\cdot\det(D-CA^{-1}B)$ | 用消去法分離兩個子空間的貢獻，是分塊行列式的統一公式 | [[Schur補矩陣]] |
| $\det\begin{bmatrix}A&B\\B&A\end{bmatrix}$（$A,B$ 均 $n\times n$） | $\det(A+B)\det(A-B)$（不需 $AB=BA$） | 對稱分塊結構可以分解為和與差，是中高難度分塊題的主要公式 | [[塊狀矩陣行列式進階]] |
| $\det\begin{bmatrix}I_m&B\\-C&I_n\end{bmatrix}$ | $\det(I_n+CB)=\det(I_m+BC)$（Sylvester 恆等式） | $m\times n$ 與 $n\times m$ 矩陣互換乘積順序，行列式（在單位矩陣修正後）相等 | [[塊狀矩陣行列式進階]] |
| $\det(A+uv^T)$，$A$ 可逆 | $(1+v^TA^{-1}u)\det(A)$；特別地 $\det(I_n+uv^T)=1+v^Tu$ | rank-1 修正只改變行列式的一個純量因子，可用 Schur 補快速推導 | [[Schur補矩陣]]、[[塊狀矩陣行列式進階]] |
| Vandermonde 型行列式 | $\prod_{i<j}(x_j-x_i)$；非零 $\Leftrightarrow$ $x_i$ **兩兩相異** | Vandermonde 的值直接反映插值節點的獨立性 | [[特殊矩陣的行列式]] |
| 三點共線條件 | $\det\begin{bmatrix}x_1&y_1&1\\x_2&y_2&1\\x_3&y_3&1\end{bmatrix}=0$ | 行列式為零 = 面積為零 = 三點共線，幾何與代數的直接橋樑 | [[特殊矩陣的行列式]] |
| **── 古典伴隨矩陣 / Cramer's Rule ──** | | | |
| 求 $A^{-1}$（行列式法） | $A^{-1}=\frac{1}{\det A}\text{adj}(A)$；$\text{adj}(A)=[\text{cof}(a_{ij})]^T$ | adj 是行列式的「分解核心」，即使 $A$ 不可逆，$A\cdot\text{adj}(A)=\det(A)I$ 仍成立 | [[古典伴隨矩陣]] |
| $\det(\text{adj}(A))$ | $=(\det A)^{n-1}$（$A$ 可逆時） | adj 把行列式放大為 $n-1$ 次方，與 $A^{-1}=\frac{1}{\det A}\text{adj}(A)$ 呼應 | [[古典伴隨矩陣]] |
| 已知 $\text{adj}(A)$，求 $A$ | 由 $\det(\text{adj}(A))=(\det A)^{n-1}$ 求 $\det(A)$，再 $A=\det(A)\cdot(\text{adj}(A))^{-1}$ | 逆向推導：adj → det(A) → A，需先確認 adj 可逆 | [[古典伴隨矩陣]] |
| 解 $Ax=b$，題目強調用行列式 | Cramer's Rule：$x_j=\det(A_j)/\det(A)$（理論用，計算效率低） | Cramer 把解的每個分量用行列式表達，主要用於理論證明，非數值計算 | [[克拉瑪法則]] |
| **── 線性方程組的解 ──** | | | |
| $Ax=0$ 有非零解 | $\text{rank}(A)<n$；$A$ 奇異；$\det(A)=0$ | 矩陣發生「維度損耗」，資訊被壓縮至零空間 | [[齊次方程組與零空間]]、[[奇異與非奇異矩陣]] |
| $m < n$，齊次系統 $Ax=0$ | **必有非零解**（方程式少於未知數，自由變數 $\geq1$） | 欠約束系統必有自由度，零空間非平凡 | [[齊次方程組與零空間]] |
| $\text{rank}(A)\neq\text{rank}([A\mid b])$ | $Ax=b$ **無解** | $b$ 不在 $CS(A)$ 內，方程組的右側超出矩陣的到達域 | [[增廣矩陣與相容性]]、[[秩與線性系統]] |
| $\text{rank}(A)=\text{rank}([A\mid b])=n$ | $Ax=b$ **唯一解** | 列滿秩 + 相容 → 零空間只含零向量，解不可平移 | [[增廣矩陣與相容性]] |
| $\text{rank}(A)=\text{rank}([A\mid b])<n$ | $Ax=b$ **無限多解**；自由變數 $=n-\text{rank}(A)$ 個 | 有解但零空間非平凡，解是一個仿射子空間 | [[增廣矩陣與相容性]]、[[基本變數與自由變數]] |
| $\text{rank}(A)=m$（行滿秩） | 對任意 $b\in F^m$，$Ax=b$ 至少有一解 | 列空間覆蓋整個目標空間，方程組永遠相容 | [[秩Rank]]、[[秩與線性系統]] |
| 非齊次 $Ax=b$ 的解集合 | 特解 $x_0$ + 零空間：$\{x_0+u\mid u\in\ker(A)\}$ | 線性算子的平移空間性質；解的唯一性取決於 $\ker(A)$ 是否為零向量 | [[解空間與解的結構]] |
| **── 向量空間 / 子空間 ──** | | | |
| 問某集合是否為子空間 | 先查 $0\in W$（否則直接否）；再驗 $\forall u,v\in W, a\in F: au+v\in W$ | 子空間 = 在線性運算下自封閉的「穩定結構」，零向量是必要門票 | [[子空間]] |
| $W_1\cup W_2$ vs $W_1+W_2$ | $\cup$ **不一定**是子空間；$+$ **必定**是子空間（包含 $\cup$ 的最小子空間） | 集合聯集破壞封閉性；和空間是最小的「包容性」封閉延伸 | [[子空間的和]] |
| $b$ 是否在 $\text{span}(S)$ 中 | 解 $Ax=b$（$A$ 的行向量 = $S$）；有解 $\Leftrightarrow$ $b\in CS(A)$ | span 成員問題等價於線性方程組相容性問題 | [[線性組合與生成空間]]、[[秩與線性系統]] |
| **── 線性獨立 ──** | | | |
| 向量個數 $>\dim(V)$ | 直接判定**線性相依** | 維度是最大獨立集的大小，超過必有冗餘 | [[線性獨立與相依]] |
| 集合含零向量 | 必**線性相依** | 零向量可以用非零係數表示（$c\cdot0=0$），獨立性立即崩潰 | [[線性獨立與相依]] |
| $n$ 個 $n$ 維向量，問獨立 | $\det[v_1,\ldots,v_n]\neq0 \Leftrightarrow$ 獨立 | 方陣行列式不為零 = Full Rank = 線性無關，三條件等價 | [[線性獨立與相依]] |
| $A$ 可逆作用獨立集 $\{v_i\}$ | $\{Av_i\}$ 仍為獨立集 | 可逆矩陣是線性同構，保持空間的獨立結構不變 | [[線性獨立與相依]]、[[矩陣乘法]] |
| 函數空間，判斷獨立 | Wronskian $W\neq0$ 於某 $x_0$ → **獨立**；但 $W=0$ **不能**反推相依 | Wronskian 是充分非必要條件，逆命題有反例 | [[Wronskian]] |
| **── 基底與維度 ──** | | | |
| $\dim(V)=k$，給定 $k$ 個向量，問是否為基底 | 只需驗**獨立**或**生成**（二選一）| $k$ 個向量在 $k$ 維空間中，「獨立」與「生成」互相蘊含 | [[基底與維度]] |
| 各空間的維度（速查） | $\dim(F^n)=n$；$\dim(M_{m\times n})=mn$；$\dim(P_n[x])=n+1$；$\dim(C^n/R)=2n$；$R^n/C$ **不是**向量空間 | 維度決定「自由度」，$R^n/C$ 不成立是因為複數純量乘出複向量但空間只含實向量 | [[基底與維度]]、[[常見向量空間]] |
| **── 四大子空間 / Rank ──** | | | |
| 求 $RS(A)$ 基底 | 列運算→REF $B$，取 $B$ 的**非零列** | 列運算保持列空間，REF 的非零列自然構成基底 | [[四大子空間的基底]] |
| 求 $\ker(A)$ 基底 | 列運算→RREF，解 $Ax=0$；自由變數各取 $1$，其餘 $0$ | 自由變數決定零空間的維度，每一個自由變數對應一個基底方向 | [[四大子空間的基底]] |
| 求 $CS(A)$ 基底 | 找 pivot 所在行數，取回**原矩陣 $A$** 的對應行向量（**不能用 $B$**） | 列運算改變行向量但不改變 pivot 位置；$B$ 的行向量不等於 $A$ 的行向量 | [[四大子空間的基底]] |
| 求 $L\ker(A)=N(A^T)$ 基底 | 對 $A^T$ 做同樣的列運算流程 | 左核 = $A^T$ 的核，轉置後問題等價 | [[四大子空間的基底]] |
| Sylvester 定理（Rank-Nullity） | $n=\text{rank}(A)+\text{nullity}(A)$；$m=\text{rank}(A)+\dim(L\ker(A))$ | 空間的總維度在 rank 與 nullity 之間守恆，是所有 rank 計算的背骨 | [[矩陣的秩]]、[[四大子空間的基底]] |
| $\text{rank}(AB)$ 的上界 | $\text{rank}(AB)\leq\min(\text{rank}(A),\text{rank}(B))$ | 乘法只能降低或維持 rank，不能增加資訊量 | [[矩陣的秩]] |
| $AB=O$，rank 關係 | $\text{rank}(A)+\text{rank}(B)\leq n$（$A$ 為 $m\times n$） | $CS(B)\subseteq\ker(A)$，$B$ 的像全部陷入 $A$ 的零空間 | [[矩陣的秩]] |
| $A=xy^T$（rank-1 矩陣） | $\text{rank}=1$；$CS=\text{span}\{x\}$；$\dim(\ker)=n-1$ | Rank-1 矩陣是最簡單的非零映射，所有輸出都壓在同一方向 | [[矩陣的秩]] |
| $P,Q$ 可逆，$\text{rank}(PA)$、$\text{rank}(AQ)$ | $=\text{rank}(A)$（可逆矩陣不改 rank） | 可逆矩陣是同構，不造成維度損耗 | [[矩陣的秩]] |
| Sylvester 不等式（進階） | $\text{rank}(AB)\geq\text{rank}(A)+\text{rank}(B)-n$ | rank 的下界：即使兩者都高，乘積最多損失 $n$ 的 rank | [[矩陣的秩]] |
| **── 維度公式 / 直和 ──** | | | |
| 求 $\dim(W_1+W_2)$ 或 $\dim(W_1\cap W_2)$ | $\dim(W_1+W_2)=\dim(W_1)+\dim(W_2)-\dim(W_1\cap W_2)$ | 容斥原理在有限維子空間上的精確版本，拼湊時用來反推交集維度 | [[和空間維度公式]] |
| 三個子空間的容斥公式 | **一般不成立**（三個子空間沒有容斥公式） | 兩個子空間的容斥源於基底擴張的唯一性，三個時失效 | [[和空間維度公式]] |
| 兩兩 $W_i\cap W_j=\{0\}$，問是否獨立 | $k\geq3$ 時**不成立**（兩兩獨立 $\neq$ 獨立子空間） | 兩兩獨立只保證局部不重疊，三個子空間可能有「三角形式」的交纏 | [[直和]] |
| 證明 $V=W_1\oplus W_2$ | 需證 (1) $V=W_1+W_2$，(2) $W_1\cap W_2=\{0\}$ | 直和 = 完美剖分：每個向量有唯一的分解方式 | [[直和]] |
| **── 座標變換 ──** | | | |
| 求 $v$ 在基底 $\beta$ 下的座標 $[v]_\beta$ | 列增廣矩陣 $[B\mid v]$ 做列運算，解出係數 | 座標是向量在特定「語言系統」下的唯一表達 | [[座標與基底變換]] |
| 求從 $\beta$ 到 $\beta'$ 的轉移矩陣 $P$ | $[B'\mid B]\xrightarrow{\text{列運算}}[I\mid P]$；$P=(B')^{-1}B$ | 轉移矩陣把舊語言翻譯成新語言，本質是基底變換的逆映射 | [[座標與基底變換]] |
