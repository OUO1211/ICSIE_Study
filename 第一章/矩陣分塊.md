---
subject: Linear Algebra
tags: [矩陣, 運算, Chapter1]
status: finished
core_mapping: "切成子矩陣，分塊相乘降低複雜度"
mantra: "大矩陣切塊塊，相乘就像小矩陣"
pattern_signal_strong:
  - condition: "矩陣有明顯零子矩陣區塊（分塊三角形式）"
    weight: 3
  - condition: "題目要求分塊乘法或分塊反矩陣"
    weight: 3
pattern_signal_weak:
  - condition: "出現列向量或行向量的組合表達式"
    weight: 1
  - condition: "題目用 A_(i) 或 A^(j) 符號"
    weight: 1
anti_signal:
  - condition: "分塊尺寸不相容（左塊行數≠右塊列數），無法做分塊乘法"
  - condition: "誤將任意切割當合法分塊，需確認內維度匹配"
---

## 定義

> **矩陣分割 (partition / block matrix)**
>
> 將較大的矩陣看成由若干較小子矩陣（方塊）組成，例如：
>
> $$A = \begin{bmatrix} A_{11} & A_{12} \\ A_{21} & A_{22} \end{bmatrix}$$

## 列向量與行向量

設 $A$ 為 $m \times n$ 矩陣：

- **列向量 (row vector)**：$A_{(i)}$ 為 $A$ 的第 $i$ 列（$1 \times n$）
- **行向量 (column vector)**：$A^{(j)}$ 為 $A$ 的第 $j$ 行（$m \times 1$）

因此：$A = \begin{bmatrix} A_{(1)} \\ A_{(2)} \\ \vdots \\ A_{(m)} \end{bmatrix} = [A^{(1)} \ A^{(2)} \ \cdots \ A^{(n)}]$

## 分塊乘法

若分割方式相容，則：

$$AB = \begin{bmatrix} A_{11}B_{11} + A_{12}B_{21} & A_{11}B_{12} + A_{12}B_{22} \\ A_{21}B_{11} + A_{22}B_{21} & A_{21}B_{12} + A_{22}B_{22} \end{bmatrix}$$

## 相關連結

- [[矩陣乘法]]
- [[分塊矩陣的反矩陣]]
- [[列行展開與分解]]
