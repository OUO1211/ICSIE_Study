---
subject: Linear Algebra
tags: [矩陣, 定義, Chapter1]
status: finished
core_mapping: "A^T=A 對稱；A^T=-A 斜對稱；任意方陣可分解為兩者之和"
mantra: "轉置等自己是對稱，轉置變負是斜版"
pattern_signal_strong:
  - condition: "出現 A^T=A 或 A^T=-A 的條件"
    weight: 3
  - condition: "題目要求將方陣分解為對稱與斜對稱部分"
    weight: 3
pattern_signal_weak:
  - condition: "出現 AA^T 或 A^TA（必為對稱矩陣）"
    weight: 1
  - condition: "題目要證明 B=(A+A^T)/2 的對稱性"
    weight: 1
anti_signal:
  - condition: "斜對稱矩陣對角項必為 0，若對角有非零值則非斜對稱"
  - condition: "複數域需改用 Hermitian（A^H=A），不用 A^T=A"
---

## 定義

> **對稱矩陣 (symmetric matrix)**：$A^T = A$，即 $a_{ij} = a_{ji}$
>
> **斜對稱矩陣 (skew-symmetric matrix)**：$A^T = -A$，即 $a_{ij} = -a_{ji}$

## 性質

- 任何方陣 $A$ 可分解為對稱與斜對稱部分：

$$A = \underbrace{\frac{A + A^T}{2}}_{\text{對稱}} + \underbrace{\frac{A - A^T}{2}}_{\text{斜對稱}}$$

- $AA^T$ 和 $A^T A$ 均為對稱矩陣
- 斜對稱矩陣的對角項必為 $0$

## 經典考題

〔104 政大資科〕**For any square matrix $A$, show that $B = \frac{A+A^T}{2}$ is symmetric.**

**解：** $B^T = \left(\frac{A + A^T}{2}\right)^T = \frac{A^T + A}{2} = B$ ✓

〔106 交大資工〕**Let $B = A - A^T$. Show that $B$ is skew-symmetric.**

**解：** $B^T = A^T - A = -B$ ✓

## 相關連結

- [[轉置矩陣]]
- [[Hermitian矩陣]]
