---
subject: Linear Algebra
tags: [矩陣, 運算, Chapter1]
status: finished
core_mapping: "純量進入矩陣，每個元素同乘"
mantra: "純量進矩陣，每格都乘到"
pattern_signal_strong:
  - condition: "出現 cA 或 αA 形式"
    weight: 3
  - condition: "題目做 (α+β)A 分配律展開"
    weight: 3
pattern_signal_weak:
  - condition: "出現 α(βA)=(αβ)A 結合律化簡"
    weight: 1
  - condition: "題目做 α(A+B) 的展開"
    weight: 1
anti_signal:
  - condition: "純量乘法 cA 不等於矩陣乘法，不可與 AC 混淆"
  - condition: "純量必須對所有元素同乘，不可只乘部分"
---

## 定義

> 若 $A = [a_{ij}]$ 為 $m \times n$ 矩陣，$c$ 為純量常數，則：
>
> $$cA = [ca_{ij}]$$

## 例子

$$A = \begin{bmatrix} 1 & 0 \\ 2 & 4 \\ 3 & 5 \end{bmatrix}, \quad 2A = \begin{bmatrix} 2 & 0 \\ 4 & 8 \\ 6 & 10 \end{bmatrix}$$

## 性質

考慮矩陣 $A$、$B$，純量 $\alpha$、$\beta$：

1. $\alpha(\beta A) = (\alpha\beta)A$
2. $(\alpha + \beta)A = \alpha A + \beta A$
3. $\alpha(A + B) = \alpha A + \alpha B$

## 相關連結

- [[矩陣加法與相等]]
- [[矩陣乘法]]
