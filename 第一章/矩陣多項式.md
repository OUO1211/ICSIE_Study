---
subject: Linear Algebra
tags: [矩陣, 定義, Chapter1]
status: finished
core_mapping: "代入矩陣套多項式，冪等/冪零時截斷級數"
mantra: "多項式代矩陣，冪零必截斷"
pattern_signal_strong:
  - condition: "出現 A^k=O（冪零）時展開 (I-A)^{-1} 為有限級數"
    weight: 3
  - condition: "出現 A^2=A（冪等）時化簡 f(A)"
    weight: 3
pattern_signal_weak:
  - condition: "題目問 (I-A)^{-1} 或 (I+A)^{-1} 的多項式形式"
    weight: 1
  - condition: "出現二項式展開 (A+I)^n"
    weight: 1
anti_signal:
  - condition: "矩陣多項式不可交換，f(A)g(A)≠g(A)f(A) 一般不成立"
  - condition: "非冪零/冪等矩陣不能截斷級數，需用 Cayley-Hamilton 或其他方法"
---

## 定義

> 設 $f(x) = c_n x^n + c_{n-1} x^{n-1} + \cdots + c_1 x + c_0$ 為多項式，$A$ 為 $n$ 階方陣，則：
>
> $$f(A) = c_n A^n + c_{n-1} A^{n-1} + \cdots + c_1 A + c_0 I$$

## 應用

利用多項式分解求反矩陣或冪次：

**例：若 $A^2 = A$（冪等），利用 $f(x) = (x+1)^n$：**

$$f(A) = (A + I)^n = \sum_{i=0}^{n} \binom{n}{i} A^i = I + (2^n - 1)A$$

（因為 $A^k = A$，$\forall k \geq 1$）〔107 台大資工〕

**例：冪零矩陣 $A^k = O$ 時：**

$$f(x) = 1 - x^k = (1-x)(1 + x + \cdots + x^{k-1})$$

$$\implies (I-A)^{-1} = I + A + A^2 + \cdots + A^{k-1}$$

## 相關連結

- [[冪零矩陣]]
- [[可逆矩陣]]
