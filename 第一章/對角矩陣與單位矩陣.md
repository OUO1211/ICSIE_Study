---
subject: Linear Algebra
tags: [矩陣, 定義, Chapter1]
status: finished
core_mapping: "辨識對角項非零，直接取倒數求反矩陣"
mantra: "對角矩陣最省力，倒數放回不費力"
pattern_signal_strong:
  - condition: "矩陣只有對角項非零，其餘全為 0"
    weight: 3
  - condition: "題目出現 diag(c1,...,cn) 記法"
    weight: 3
pattern_signal_weak:
  - condition: "出現 tr(In)=n 的跡數計算"
    weight: 1
  - condition: "題目問 AI=A 或 IA=A 的單位矩陣性質"
    weight: 1
anti_signal:
  - condition: "非方陣（對角矩陣定義僅對方陣成立）"
  - condition: "對角項有零，無法直接取倒數求反矩陣"
---

## 定義

> **對角矩陣 (diagonal matrix)**
>
> 若一 $n$ 階方陣的非對角項均為 $0$，稱之為對角矩陣，記成 $\operatorname{diag}(d_1, \ldots, d_n)$。

> **單位矩陣 (identity matrix)**
>
> 若對角矩陣的對角項全為 $1$，稱之為單位矩陣，記成 $I_n$。

## 例子

$$A = \begin{bmatrix} 1 & 0 \\ 0 & 2 \end{bmatrix} = \operatorname{diag}(1, 2)$$

$$I_3 = \begin{bmatrix} 1 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{bmatrix}$$

## 性質

- 對任意 $A_{m \times n}$：$A I_n = A$，$I_m A = A$
- $\operatorname{diag}(c_1, \ldots, c_n)^{-1} = \operatorname{diag}(c_1^{-1}, \ldots, c_n^{-1})$（各 $c_i \neq 0$）
- $\operatorname{tr}(I_n) = n$

## 相關連結

- [[特殊矩陣類型]]
- [[三角矩陣]]
- [[跡數Trace]]
